language: go
go:
  - 1.4.2
  - 1.5.1
  - release
  - tip

sudo: false

env:
  - EK_TEST_PORT=8080

before_install:
  - go get gopkg.in/check.v1

script:
  - .travis/script.sh .

after_success:
  - bash <(curl -s https://codecov.io/bash)
